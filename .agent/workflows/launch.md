---
description: Launch the entire application stack (Docker, Prisma, Next.js)
---

This workflow automates the startup of the application.

// turbo
1. Run the unified launch command:
```bash
npm run launch
```

2. Wait for the Next.js server to start (it will log "Ready in ...").

3. The application is now accessible at http://localhost:3000.
